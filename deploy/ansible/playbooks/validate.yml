---
- name: Validate ChooseYourTube Ansible variables
  hosts: chooseyourtube
  become: false
  gather_facts: false
  tasks:
    - name: Run deployment variable validation
      ansible.builtin.include_role:
        name: chooseyourtube
        tasks_from: validate.yml
